// Shared between Checklist (writes checked-off state) and ClearListButton
// (wipes it) so "Clear list" can reset both the URL selection and the
// checked-off progress in one action.
export const SHOPPING_CHECKED_STORAGE_KEY = "cook-cli:shopping-checked";
